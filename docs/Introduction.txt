Introduction 

Remover/ajustar “optional window-based temporal aggregation”: hoje o seguro é dizer que TTD/coverage/FPR são calculados por
    ordenação temporal (Timestamp) e não “sliding/multiscale early detection”.
  - Explicitar que o Non‑IID do paper é principalmente quantity skew (Dirichlet) e “hetero_device” por grupos/volume, não feature/
    label-skew (pra não prometer heterogeneidade mais forte do que existe).
  - (Opcional, mas recomendo) Enxugar as contribuições para 3–4 bullets (juntar 2+3 e juntar 4+5) e padronizar as citações para
    \cite{...}.



imagens
Se no texto do paper você usa T em vez de R, alinhe (mas R está ótimo).

Garanta que a fonte/tamanho continua legível quando reduzir pra largura de 1 coluna (se ficar pequeno, aumente 5–10% o texto do callout “payload …”).
